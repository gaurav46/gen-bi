{
  "name": "gen-bi-test-db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "migrate": "knex migrate:latest",
    "seed": "knex seed:run",
    "db:setup": "ts-node reset-db.ts && knex migrate:latest && knex seed:run",
    "rollback": "knex migrate:rollback --all"
  },
  "dependencies": {
    "knex": "^3.1.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@types/pg": "^8.16.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
